name: Waiting for answer.

on:
  issue_comment:
    types: [created]
  schedule:
    - cron: '30 0 * * *'

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  waiting-for-answer:
    runs-on: ubuntu-slim
    steps:
      - uses: lee-dohm/no-response@v0.5.0
        with:
          token: ${{ github.token }}
          responseRequiredLabel: waiting for answer
